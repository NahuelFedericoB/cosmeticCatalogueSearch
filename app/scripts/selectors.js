export const getSearchBar = (state) => state.elc.searchBar.searchCriteria;

export const getCatalogue = (state) => state.elc.catalogue.catalogue;

export const getCatalogueIsLoading = (state) => state.elc.catalogue.isLoading;
